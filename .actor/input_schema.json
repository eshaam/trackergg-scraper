{
    "actorOutputSchemaVersion": 1,
    "title": "Trackergg Scraper Output",
    "properties": {
        "dataset": {
            "type": "string",
            "title": "Player Statistics",
            "description": "Scraped player statistics from tracker.gg",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=player-stats"
        }
    }
}